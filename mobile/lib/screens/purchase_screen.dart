import 'package:flutter/material.dart';

class PurchaseRoot extends StatelessWidget {
  const PurchaseRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Purchase Screen', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
